{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
{% set page_actions %}
    <a class="btn btn-primary" href="{{ create_url }}">{{ 'Add warehouse'|trans({}, 'Modules.Ppomniva.Admin') }}</a>
{% endset %}

{{ include('@Modules/ppomniva/views/templates/admin/partials/_admin_menu.html.twig', {
    active_tab: 'warehouses',
    page_title: 'Warehouses'|trans({}, 'Modules.Ppomniva.Admin'),
    page_description: 'Sender addresses used on shipments and labels. The default warehouse is used when no specific warehouse is set on an order.'|trans({}, 'Modules.Ppomniva.Admin'),
    multistore_blocked: multistore_blocked,
    page_actions: page_actions,
}) }}

{% if not multistore_blocked %}
<div class="ppo-shell">
    <div class="ppo-card">
        <div class="ppo-card__header">
            <h3 class="ppo-card__title">{{ 'Warehouse list'|trans({}, 'Modules.Ppomniva.Admin') }}</h3>
            <span class="ppo-card__help">{{ 'Each shipment needs a sender block; if you operate from more than one location, add them here and pick a default.'|trans({}, 'Modules.Ppomniva.Admin') }}</span>
        </div>
        <div class="ppo-card__body">
            {% if warehouses|length == 0 %}
                <div class="ppo-empty">
                    <h4 class="ppo-empty__title">{{ 'No warehouses yet'|trans({}, 'Modules.Ppomniva.Admin') }}</h4>
                    <p class="ppo-empty__body">{{ 'Add at least one warehouse before generating shipments. Without a warehouse, label generation will fail.'|trans({}, 'Modules.Ppomniva.Admin') }}</p>
                    <a class="btn btn-primary" href="{{ create_url }}">{{ 'Add your first warehouse'|trans({}, 'Modules.Ppomniva.Admin') }}</a>
                </div>
            {% else %}
                <table class="ppo-table">
                    <thead>
                        <tr>
                            {% if show_shop_column|default(false) %}
                                <th>{{ 'Shop'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                            {% endif %}
                            <th>{{ 'Name'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                            <th>{{ 'Country'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                            <th>{{ 'City'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                            <th>{{ 'Address'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                            <th>{{ 'ZIP'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                            <th>{{ 'Contact'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                            <th>{{ 'Phone'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                            <th>{{ 'Default'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for w in warehouses %}
                            <tr>
                                {% if show_shop_column|default(false) %}
                                    <td>
                                        {% if w.id_shop > 0 %}
                                            <span class="ppo-badge">{{ shop_names[w.id_shop]|default('#' ~ w.id_shop) }}</span>
                                        {% else %}
                                            <span class="ppo-badge ppo-badge--info">{{ 'Global'|trans({}, 'Modules.Ppomniva.Admin') }}</span>
                                        {% endif %}
                                    </td>
                                {% endif %}
                                <td>
                                    <strong>{{ w.name }}</strong>
                                    {% if w.company_code %}<br><small style="color: var(--ppo-text-muted);">{{ 'Company code'|trans({}, 'Modules.Ppomniva.Admin') }}: {{ w.company_code }}</small>{% endif %}
                                </td>
                                <td>{{ w.country_code }}</td>
                                <td>{{ w.city }}</td>
                                <td>{{ w.address }}</td>
                                <td>{{ w.zip_code }}</td>
                                <td>{{ w.contact }}</td>
                                <td>{{ w.phone }}</td>
                                <td>
                                    {% if w.is_default %}
                                        <span class="ppo-badge ppo-badge--primary">{{ 'Default'|trans({}, 'Modules.Ppomniva.Admin') }}</span>
                                    {% else %}
                                        <form method="post" action="{{ path('ps_ppomniva_warehouse_set_default', { id: w.id_ppomniva_warehouse }) }}" data-ppo-loading style="display:inline;">
                                            <button type="submit" class="btn btn-link" style="padding: 0; font-size: var(--ppo-text-sm);">{{ 'Set default'|trans({}, 'Modules.Ppomniva.Admin') }}</button>
                                        </form>
                                    {% endif %}
                                </td>
                                <td class="ppo-row-actions">
                                    <a class="btn btn-secondary" href="{{ path('ps_ppomniva_warehouse_edit', { id: w.id_ppomniva_warehouse }) }}">{{ 'Edit'|trans({}, 'Modules.Ppomniva.Admin') }}</a>
                                    <form method="post" action="{{ path('ps_ppomniva_warehouse_delete', { id: w.id_ppomniva_warehouse }) }}"
                                          onsubmit="return confirm('{{ 'Delete this warehouse?'|trans({}, 'Modules.Ppomniva.Admin') }}');">
                                        <button type="submit" class="btn btn-danger">{{ 'Delete'|trans({}, 'Modules.Ppomniva.Admin') }}</button>
                                    </form>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            {% endif %}
        </div>
    </div>
</div>
{% endif %}
{% endblock %}
