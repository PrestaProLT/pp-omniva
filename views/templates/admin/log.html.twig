{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
{{ include('@Modules/ppomniva/views/templates/admin/partials/_admin_menu.html.twig', {
    active_tab: 'logs',
    page_title: 'Logs'|trans({}, 'Modules.Ppomniva.Admin'),
    page_description: 'Audit trail of every Omniva API interaction (errors, parse failures, transport failures). Click a row to inspect the full request and response.'|trans({}, 'Modules.Ppomniva.Admin'),
}) }}

<div class="ppo-shell">
    <div class="ppo-card">
        <div class="ppo-card__body">
            <form method="get" action="{{ index_url }}" class="ppo-filters" style="margin-bottom: var(--ppo-space-3);">
                <input type="hidden" name="_token" value="{{ app.request.query.get('_token')|default('') }}">
                <div class="form-group">
                    <label>{{ 'Level'|trans({}, 'Modules.Ppomniva.Admin') }}</label>
                    <select name="level">
                        <option value="">{{ 'Any'|trans({}, 'Modules.Ppomniva.Admin') }}</option>
                        {% for lvl in levels %}
                            <option value="{{ lvl }}"{% if filters.level == lvl %} selected{% endif %}>{{ lvl }}</option>
                        {% endfor %}
                    </select>
                </div>
                <div class="form-group">
                    <label>{{ 'Source'|trans({}, 'Modules.Ppomniva.Admin') }}</label>
                    <select name="source">
                        <option value="">{{ 'Any'|trans({}, 'Modules.Ppomniva.Admin') }}</option>
                        {% for src in sources %}
                            <option value="{{ src }}"{% if filters.source == src %} selected{% endif %}>{{ src }}</option>
                        {% endfor %}
                    </select>
                </div>
                <div class="form-group">
                    <label>{{ 'Error code'|trans({}, 'Modules.Ppomniva.Admin') }}</label>
                    <input type="number" name="error_code" value="{{ filters.error_code }}" min="1" style="width: 90px;">
                </div>
                <div class="form-group">
                    <label>{{ 'Order ID'|trans({}, 'Modules.Ppomniva.Admin') }}</label>
                    <input type="number" name="id_order" value="{{ filters.id_order }}" min="1" style="width: 110px;">
                </div>
                <div class="form-group">
                    <label>{{ 'From'|trans({}, 'Modules.Ppomniva.Admin') }}</label>
                    <input type="date" name="date_from" value="{{ filters.date_from }}">
                </div>
                <div class="form-group">
                    <label>{{ 'To'|trans({}, 'Modules.Ppomniva.Admin') }}</label>
                    <input type="date" name="date_to" value="{{ filters.date_to }}">
                </div>
                <div class="form-group ppo-filters__search">
                    <label>{{ 'Search message / endpoint'|trans({}, 'Modules.Ppomniva.Admin') }}</label>
                    <input type="text" name="q" value="{{ filters.q }}">
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary">{{ 'Filter'|trans({}, 'Modules.Ppomniva.Admin') }}</button>
                    <a class="btn btn-secondary" href="{{ index_url }}">{{ 'Reset'|trans({}, 'Modules.Ppomniva.Admin') }}</a>
                </div>
            </form>

            <p style="color: var(--ppo-text-muted); margin: 0 0 var(--ppo-space-2);">{{ 'Showing %shown% of %total% entries.'|trans({'%shown%': logs|length, '%total%': total}, 'Modules.Ppomniva.Admin') }}</p>

            {% if logs|length == 0 %}
                <div class="ppo-empty">
                    <h4 class="ppo-empty__title">{{ 'No log entries match the current filters'|trans({}, 'Modules.Ppomniva.Admin') }}</h4>
                    <p class="ppo-empty__body">{{ 'Reset filters to see everything, or wait for the next API call.'|trans({}, 'Modules.Ppomniva.Admin') }}</p>
                </div>
            {% else %}
            <table class="ppo-table">
                <thead>
                    <tr>
                        <th style="width: 140px;">{{ 'Date'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                        <th style="width: 80px;">{{ 'Level'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                        <th style="width: 60px;">{{ 'Code'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                        <th style="width: 220px;">{{ 'Source'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                        <th>{{ 'Message'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                        <th style="width: 80px;">{{ 'Order'|trans({}, 'Modules.Ppomniva.Admin') }}</th>
                    </tr>
                </thead>
                <tbody>
                    {% for log in logs %}
                    {% set badge_cls = log.level == 'error' ? 'danger' : (log.level == 'warning' ? 'warn' : 'info') %}
                    <tr class="is-clickable" data-log-id="{{ log.id_ppomniva_log }}">
                        <td>{{ log.date_add }}</td>
                        <td><span class="ppo-badge ppo-badge--{{ badge_cls }}">{{ log.level }}</span></td>
                        <td><code>{{ log.error_code|default('—') }}</code></td>
                        <td><code style="font-size: 11px;">{{ log.source }}</code><br><small style="color: var(--ppo-text-muted);">{{ log.endpoint }}</small></td>
                        <td style="max-width: 540px; white-space: pre-line;">
                            {{ log.message }}
                            {# Verbatim text(s) from Omniva's response XML.
                               Pulled out of the structured `context.errors[]`
                               list so the operator can see what the API
                               literally said without expanding the row. #}
                            {% if log.omniva_raw|default([])|length > 0 %}
                                <div class="ppo-log-raw">
                                    <span class="ppo-log-raw__label">{{ 'Omniva response'|trans({}, 'Modules.Ppomniva.Admin') }}:</span>
                                    {% for err in log.omniva_raw %}
                                        <code class="ppo-log-raw__line">[{{ err.code|default('—') }}] {{ err.message }}</code>
                                    {% endfor %}
                                </div>
                            {% endif %}
                            {# Inline raw payload viewers — collapsed by
                               default so the table stays scannable, but the
                               XML lives in the page source so opening it is
                               instant and works for many rows at once. #}
                            {% if log.request_body %}
                                <details class="ppo-log-payload" data-stop-propagation>
                                    <summary>{{ 'Request'|trans({}, 'Modules.Ppomniva.Admin') }} <span class="ppo-log-payload__size">({{ log.request_body|length }} chars)</span></summary>
                                    <pre>{{ log.request_body }}</pre>
                                </details>
                            {% endif %}
                            {% if log.response_body %}
                                <details class="ppo-log-payload" data-stop-propagation>
                                    <summary>{{ 'Response'|trans({}, 'Modules.Ppomniva.Admin') }} <span class="ppo-log-payload__size">({{ log.response_body|length }} chars)</span></summary>
                                    <pre>{{ log.response_body }}</pre>
                                </details>
                            {% endif %}
                        </td>
                        <td>{{ log.id_order > 0 ? '#' ~ log.id_order : '' }}</td>
                    </tr>
                    <tr class="ppo-log-detail-row" data-detail-for="{{ log.id_ppomniva_log }}" style="display:none;">
                        <td colspan="6"><div class="ppo-detail-pane" data-detail-pane>{{ 'Loading…'|trans({}, 'Modules.Ppomniva.Admin') }}</div></td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
            {% endif %}

            {% if page_count > 1 %}
            <div class="ppo-pagination">
                {% set query = filters|filter(v => v != '') %}
                {% if page > 1 %}
                    <a class="btn btn-secondary btn-sm" href="{{ path('ps_ppomniva_log', query|merge({'page': page - 1})) }}">‹ {{ 'Previous'|trans({}, 'Modules.Ppomniva.Admin') }}</a>
                {% endif %}
                <span>{{ 'Page %current% of %total%'|trans({'%current%': page, '%total%': page_count}, 'Modules.Ppomniva.Admin') }}</span>
                {% if page < page_count %}
                    <a class="btn btn-secondary btn-sm" href="{{ path('ps_ppomniva_log', query|merge({'page': page + 1})) }}">{{ 'Next'|trans({}, 'Modules.Ppomniva.Admin') }} ›</a>
                {% endif %}
            </div>
            {% endif %}
        </div>
        <div class="ppo-card__footer">
            <form method="post" action="{{ cleanup_url }}"
                  onsubmit="return confirm('{{ 'Delete log entries older than the chosen retention?'|trans({}, 'Modules.Ppomniva.Admin') }}');"
                  style="display: flex; gap: var(--ppo-space-2); align-items: end; margin: 0;"
                  data-ppo-loading>
                <div class="form-group" style="margin: 0;">
                    <label style="font-size: var(--ppo-text-xs); color: var(--ppo-text-muted); margin-bottom: 2px;">{{ 'Retention days (0 = clear all)'|trans({}, 'Modules.Ppomniva.Admin') }}</label>
                    <input type="number" name="retention_days" value="30" min="0" style="width: 110px; padding: var(--ppo-space-1) var(--ppo-space-2); border: 1px solid var(--ppo-border-strong); border-radius: var(--ppo-radius-sm);">
                </div>
                <button type="submit" class="btn btn-danger">{{ 'Clean up'|trans({}, 'Modules.Ppomniva.Admin') }}</button>
            </form>
        </div>
    </div>
</div>

<script>
(function () {
    var detailUrl = {{ detail_url|json_encode|raw }};
    var cache = {};

    // Prevent clicks inside the inline payload viewers (and any other
    // interactive child) from bubbling up to the row and toggling the
    // AJAX-loaded context detail.
    document.querySelectorAll('.ppo-table tbody tr[data-log-id] details').forEach(function (d) {
        d.addEventListener('click', function (e) { e.stopPropagation(); });
    });

    document.querySelectorAll('.ppo-table tbody tr[data-log-id]').forEach(function (row) {
        row.addEventListener('click', function (e) {
            // Ignore bubbling clicks that came from a <details>/<summary>
            // inside the row — those toggle their own payload, not the row.
            if (e.target.closest('details')) return;
            var id = row.dataset.logId;
            var detailRow = document.querySelector('.ppo-log-detail-row[data-detail-for="' + id + '"]');
            if (!detailRow) return;
            if (detailRow.style.display === 'table-row') { detailRow.style.display = 'none'; return; }
            document.querySelectorAll('.ppo-log-detail-row').forEach(function (r) { r.style.display = 'none'; });
            detailRow.style.display = 'table-row';
            var pane = detailRow.querySelector('[data-detail-pane]');
            if (cache[id]) { pane.innerHTML = cache[id]; return; }
            pane.textContent = 'Loading…';
            fetch(detailUrl + '?id=' + encodeURIComponent(id), { headers: { 'Accept': 'application/json' }, credentials: 'same-origin' })
                .then(function (r) { return r.json(); })
                .then(function (data) {
                    if (!data.success) { pane.textContent = data.message || 'Failed to load detail.'; return; }
                    var l = data.log;
                    var html = '';
                    if (l.request_body)  html += '<h5>Request</h5><pre>' + esc(l.request_body) + '</pre>';
                    if (l.response_body) html += '<h5>Response</h5><pre>' + esc(l.response_body) + '</pre>';
                    if (l.context)       html += '<h5>Context</h5><pre>' + esc(typeof l.context === 'string' ? l.context : JSON.stringify(l.context, null, 2)) + '</pre>';
                    if (!html) html = '<em>No request/response/context captured.</em>';
                    cache[id] = html; pane.innerHTML = html;
                })
                .catch(function (err) { pane.textContent = 'Error: ' + err.message; });
        });
    });

    function esc(s) { var div = document.createElement('div'); div.textContent = String(s == null ? '' : s); return div.innerHTML; }
})();
</script>
{% endblock %}
